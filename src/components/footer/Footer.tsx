import './footer.scss'

const Footer = () => {
    return (
        <div className='footer'>
            <span>Dashboard</span>
            <span>Welcome to Dashboard</span>
        </div>
    );
}

export default Footer;